'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

var encode = exports.encode = function encode(bible) {
  return bible.map(function (book) {
    return book.map(function (chapter) {
      return chapter.map(function (_ref) {
        var n = _ref.n;
        var txt = _ref.txt;
        return n + '|' + txt;
      }).join('');
    }).join('||');
  }).join('|||');
};

var decode = exports.decode = function decode(bible) {
  return bible.split('|||').map(function (book) {
    return book.split('||').map(unflattenChapter);
  });
};

var unflattenChapter = function unflattenChapter(chapter) {
  return chapter.replace(/\d+\|/g, function (match) {
    return '||' + match;
  }).substr(2).split('||').map(function (verse) {
    var _verse$split = verse.split('|');

    var _verse$split2 = _slicedToArray(_verse$split, 2);

    var n = _verse$split2[0];
    var txt = _verse$split2[1];

    return { n: n, txt: txt };
  });
};